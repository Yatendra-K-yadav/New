#include<stdio.h>
int main()
{
    int length,width,height,radius,area;
    printf("Enter radius of circle:\n");
    scanf("%d",&radius);
    area= 3.14*radius*radius;
    printf("area%d",area);

    return 0;
}
